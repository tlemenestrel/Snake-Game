class Corps():
    def position ():
        self.position_2 = corps1.get_rect()
    def load ():
        #On charge le corps du serpent
        corps1 = pygame.image.load("corps.png").convert_alpha()
        corps1 = pygame.transform.scale(corps1, (25,25))
